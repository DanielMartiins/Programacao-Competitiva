#include <bits/stdc++.h>
#define vi vector<int>
using namespace std;

void solve() {
    /*
    int n, ans;
    ans = 0;

    cin >> n;

    int a, b;

    for (a = n-1; a > 0; a--)
        for (b = 1; b < n; b++) {
            if (n-a)
        }
    */
   int n;
   cin >> n;

   cout << n - 1 << '\n';
}

int main() {
    //ios::sync_with_stdio(false);
    //cin.tie(0);

    int t;
    cin >> t;

    while(t--) {
        solve();
    }
}